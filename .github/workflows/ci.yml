name: CI
on:
  push:
    branches: [ "main" ]

jobs:
  run-test:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: Run test script
        run: |
          chmod +x test.sh
          ./test.sh

